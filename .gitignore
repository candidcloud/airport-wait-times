# Python compilation files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Mac / System junk files
.DS_Store
Thumbs.db

# The data file generated by actions (optional, but good practice so local tests don't overwrite server data)
# Note: Keep this commented out for now since GitHub Actions will push to this file anyway, 
# but it's good to have standard ignores in place.
# data.json